import React from 'react';
import Router from 'next/router';

export default function Error() {
    // Reroute automatically (in the future)
    return (
        <div>
            This page doesn't exist!
        </div>
    )
};
